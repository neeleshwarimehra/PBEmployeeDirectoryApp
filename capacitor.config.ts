import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.prasarbharati.employeedirectory",
  appName: "Employee Directory",
  webDir: "dist",
};

export default config;
